<nav class="navbar navbar-expand-lg navbar-light bg-light navbar-default navbar-fixed-top">
  <a class="navbar-brand" href="/">
    <!--<img class='icon' src="images/icon.png"/>-->Hitch It
  </a>
    <ul class="navbar-nav ml-auto">
      <li class="nav-item">
        <a class="nav-link" href="/">Accueil</a>
      </li>
      <% if (!user) { %>
        <li class="nav-item">
          <a class="nav-link" href="/login">Connexion</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/signup">Inscription</a>
        </li>
      <% }else{ %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle nav-link" data-toggle='dropdown'>
            <% if (user.profile.picture) { %>
              <img class="profile-picture" src="<%= user.profile.picture %>" alt="">
            <% } else { %>
              <img class="profile-picture" src="images/avatar.png" alt="">
            <% } %> 
            <span><%= user.profile.name || user.email || user.id %></span>
          </a>
          <div class="dropdown-menu">
            <a class="dropdown-item" href="/logout">Log Out</a>
            <div class="dropdown-divider"></div>
            <form action="/account/delete" method="POST" onsubmit="return confirm('Are you sure you want to delete your account?');">
              <button type="submit" class="btn">Delete Account</button>
          </form>
          </div>
        </li>
      <% } %> 
    </ul>
</nav>

<%- include('flash'); %>